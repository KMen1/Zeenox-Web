"use client";

import { createTheme } from "@mantine/core";
export const theme = createTheme({
  defaultRadius: "lg",
  colors: {
    dark: [
      "#ededed",
      "#acaebf",
      "#8c8fa3",
      "#4d4d4d",
      "#3c3c3c",
      "#1e1e1e",
      "#111111",
      "#000000",
      "#000000",
      "#000000",
    ],
  },
});
